const arr = [1,2,3,'a','b']

// push pull unshift shift




// const 原始类型，值不能被改变。const 数组、对象，只要不改变其地址（新增一个对象），就可以改变地址对应的值。
// const arr = [1, 2, 3];

// // ✅ 可以改变地址指向的数据（堆中的内容）
// arr.push(4);        // [1, 2, 3, 4] - 允许
// arr[0] = 100;       // [100, 2, 3] - 允许
// arr.pop();          // [1, 2] - 允许

// // ❌ 不能改变地址本身
// arr = [4, 5, 6];    // ❌ 报错！不能改变arr指向的新地址

