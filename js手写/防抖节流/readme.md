# 应用场景
- 当用户的行为导致需要向 服务端(后端) 发送请求的时候 (高并发)，需要控制用户的行为，就要考虑加 防抖 了

## 防抖
- 要解决这个问题，可以给 html 添加 disabled 属性。但是如果我检查这个按钮，直接在浏览器把 disabled 删掉，就没用了

防抖机制： **在规定时间内，没有新的事件触发，才执行**

> 定时器函数执行完之后不用 return 也会有结果，会返回 当前页面运行的定时器次序，v8会默认给定时器安排一个序号，第几号沙漏。

## 节流
节流机制： **在规定的时间内只触发一次**